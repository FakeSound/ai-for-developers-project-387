# Локальное окружение разработки — в образе не нужно и раздувает контекст сборки.
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Тесты запускаются локально (`npm run api:test`): им нужен openapi/openapi.yaml
# из корня репозитория, которого в контексте сборки бэкенда нет.
tests/
pytest.ini
